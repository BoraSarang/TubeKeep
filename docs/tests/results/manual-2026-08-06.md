# 수동 테스트 결과 (2026-08-06)

> 자동화 테스트 `./scripts/test-core.sh` (23 PASS / 0 FAIL) 통과 후 진행.

## 자동 검증 완료 (CLI)

- **TC-SET-01 설정 유지**: `~/Library/Containers/com.borasarang.tubekeeper/Data/Library/Preferences/com.borasarang.tubekeeper.plist`의 `appSettings`에 `idleAutoSummary:true` / `idleAutoPodcast:false`(v3.2 T-1072), `idleAutoSummary` 저장 확인. 앱 실행 후 재시작에도 유지됨.
- **TC-DL-01 다운로드 파이프라인**: yt-dlp(2026.07.04)로 `dQw4w9WgXcQ` 메타데이터 추출 → ffmpeg로 10초 구간 다운로드 성공 → ffprobe 검증(duration=10.04s, size=496546). 다운로드 완료 확인.
  - 참고: youtube-dl 테스트 영상 `BaW_jenozKc`는 YouTube에서 삭제되어 unavailable.
- **TC-DL-04 오류 처리**: 존재하지 않는 영상 ID로 yt-dlp 실행 시 `ERROR: Video unavailable` 출력 후 종료(크래시 없음) — 앱은 에러코드 래핑으로 한글 메시지 표시 경로 확인.

## 통과

- TC-SET-01, TC-DL-01(파이프라인), TC-DL-04(오류 종료)

## 남은 수동 확인 필요 (앱 UI 조작)

- TC-PL-01~05: 실제 재생(mpv) / 이어보기 재개 위치·배지 / 5초·5% 제외 규칙 / 클립 A·B 구간 저장 / 전역 단축키
- TC-AI-01~05: 요약·태그·팟캐스트·QnA 실제 API 호출 / 유휴 AI 배치(설정 on → 자막 후 자동 생성)
- TC-CH-01~04: 채널 추가·프리셋 반영(해상도/MP3/자막/일일 한도)·재생목록 구독·알림
- TC-DL-05/06: 중복 배지·스킵 토스트 / 큐 드래그 정렬 후 재시작 유지
- TC-DL-02/03: 다운로드 취소(.part 정리)·일시정지/재개
- TC-IDLE-01: 유휴 5분 자막 (활동 시 즉시 중단)
- TC-SET-02~05: 마이그레이션 / 알림음·메뉴바 알림 / 항목 삭제 시 연관 데이터 정리 / 다운로드 중 ⌘Q 후 잔여 프로세스 없음

## 비고

- `docs/tests/manual-checklist.md` 전체 항목(26종)은 UI 조작 필요 → 앱에서 직접 확인 후 체크 표시.
